
# 使用说明

对于并不想了解代码原理的朋友们，该代码只需要**改变你所希望存储的文件地址以及文件名**即可直接使用。

# 1. 代码架构

本项目为了方便初学者理解，一共划分为了三个部分，首先get_list.py用于获取漫画的章节及其对应的url链接，并将内容存为csv文件内，方便后续get_img.py进行读取利用；之后是get_img.py，该文件是用于爬取对应的图片，也是代码的核心部分；最后是img2pdf.py正如其名字一样他是用于将爬取的图片转化为pdf毕竟如果想要在其它设备上来看漫画的话，一堆img可不太方便。
